import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const UserCenter: React.FC = () => {
  return (
    <Card>
      <Title level={2}>用户中心</Title>
      {/* Add your user center content here */}
    </Card>
  );
};

export default UserCenter;
